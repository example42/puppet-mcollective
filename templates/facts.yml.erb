---
<%
        mco_facts = scope.to_hash.reject do |k,v|
                k.to_s =~ /(uptime|timestamp|free|require|last_run|path)/
        end
        mco_yaml = String.new
        mco_facts.each_key.sort.each do |fact|
                mco_yaml << " #{fact}: \"#{mco_facts[fact].to_s.gsub('"', '\"')}\"\n"
        end
-%>
<%= mco_yaml -%> 